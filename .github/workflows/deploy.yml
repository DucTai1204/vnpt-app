# Build MiniApp SAN thành web tĩnh rồi publish lên GitHub Pages.
#
# Backend (../vnpt-api) KHÔNG nằm trong repo này — bản Pages gọi sang một
# domain khác, khai bằng VITE_API_BASE_URL trong bước Build bên dưới.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  # Cho phép bấm chạy tay sau khi đổi URL backend (ngrok free đổi domain mỗi
  # lần khởi động lại) mà không cần commit rỗng.
  workflow_dispatch:

# Chỉ cấp đúng quyền cần để đẩy artifact lên Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Một lần deploy tại một thời điểm; không hủy lần đang chạy dở.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build
        env:
          # Địa chỉ backend khai thẳng ở đây, KHÔNG đọc từ repository variable.
          # Trước đây đọc từ vars.VITE_API_BASE_URL, và chính chỗ đó gây lỗi:
          # biến còn giữ URL ngrok đã tắt nên bản deploy mới vẫn gọi vào địa chỉ
          # chết mà nhìn file này không thấy được. Để trong file thì đổi URL là
          # một dòng sửa có trong lịch sử git, ai review cũng thấy.
          VITE_API_BASE_URL: https://vnpt-san-api.onrender.com
          # "." = lấy ảnh từ public/media đã đóng gói sẵn, cùng origin với trang.
          VITE_MEDIA_BASE_URL: '.'
        run: npm run build

      - name: SPA fallback
        # App là SPA một trang; 404.html trỏ về index.html để refresh ở
        # đường dẫn con không rơi vào trang 404 của GitHub.
        run: cp dist/index.html dist/404.html

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
